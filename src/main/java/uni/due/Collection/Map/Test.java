package uni.due.Collection.Map;

public class Test {

    /*
    * A. HashMap遍历
    *
    * 1.使用Iterator迭代器:
    * Iterator<String> it = animal.values().iterator();
    * while(it.hasNext()){
    * sout(it.next())
    * }
    *
    * 2.使用Set EntrySet():
    * Set<Entry<String,String>> set = animal.entrySet();
    * sout(set.getKey()+ " "+set.getValue());
    *
    * B. HashMap查找key
    * String input = console.next();
    * Set<String> keySet= animal.keySet();
    * for(String key:ketSet)
    * if(input.equals(key))
    * sout(animal.get(key));
    *
    * C.
    * */
}
